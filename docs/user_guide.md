## Keeping Grok in Sync
- After each chat, condense it with Grok using the condense prompt from `prompts.md` and save to `state.json`.
- Start new sessions by sharing the latest condensed state if needed.